import 'package:flutter/material.dart';

class UploadEdit extends StatelessWidget {
  const UploadEdit({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Upload Edit',
        style: TextStyle(fontSize: 15, fontWeight: FontWeight.bold),
      ),
    );
  }
}