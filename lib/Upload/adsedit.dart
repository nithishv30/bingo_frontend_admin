import 'package:flutter/material.dart';

class AdsEdit extends StatelessWidget {
  const AdsEdit({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'ads edit',
        style: TextStyle(fontSize: 15, fontWeight: FontWeight.bold),
      ),
    );
  }
}